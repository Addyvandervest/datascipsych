# %% [markdown]
# # Chapter 1 Assignment
# 
# Please fill in blanks in the *Answer* sections of this notebook. To check your answer for a problem, run the Setup, Answer, and Result sections. DO NOT MODIFY SETUP OR RESULT CELLS. See the [README](https://github.com/mortonne/datascipsych) for instructions on setting up a Python environment to run this notebook.
# 
# Write your answers for each problem. Then restart the kernel, run all cells, and then save the notebook. Upload your notebook to Canvas.
# 
# If you get stuck, read through the other notebooks in this directory, ask us for help in class, or ask other students for help in class or on the weekly discussion board.

# %% [markdown]
# ## Sample problem (not graded)
# First, let's go through a sample problem to show you how this works. You'll see problems below to fill out. They will have instructions, often some sort of data to work with, and a space to fill in your answer. For example:

# %% [markdown]
# ### Problem
# Use f-strings to greet a user. Given a greeting string and a user string, make a string of `"[greeting] [user]"` using f-strings. Assign your string to a variable called `message`.

# %% [markdown]
# #### Setup
# There will usually be a *Setup* cell that defines some variables. Often it will initialize a variable that you need to modify. Here, the `message` variable will hold the greeting string that you will make. We initialize it to `None` in the *Setup* cell so that the *Result* cell will run without an error even if you haven't written your answer yet.

# %%
greeting = "hello"  # string to greet the user
user = "world"  # user to greet
message = None  # you will overwrite this variable with your message

# %% [markdown]
# #### Answer
# This is the only part that you should change. You will edit the code cell in the *Answer* section to add your code (you can replace the `# your code here` placeholder).

# %%
# your code here

# %% [markdown]
# #### Result
# 
# This part is just here to display the results of running your code and run checks to see if it is (probably) correct. You won't edit this part.

# %%
if message is not None:
    # this should print your variable
    print(message)

    # this should not throw any errors
    assert message == "hello world"

# %% [markdown]
# ### Writing a solution

# %% [markdown]
# The `message` variable is initially defined as `None` (this makes it so the *Result* section runs without errors, even if you haven't finished the problem yet). You should put your code in the *Answer* section to assign `message` to the correct value. For example, here is a solution:

# %%
message = f"{greeting} {user}"

# %% [markdown]
# If you placed this in the *Answer* section, you would get full credit for this problem. Try copying and pasting the above code into the *Answer* section, and see how the *Result* section changes to display your `message` variable and check that it has the correct value. Note that the way you solve a problem matters; if you just wrote `message = "hello world"`, that would be incorrect, because you did not follow the directions to use an f-string to construct the string.

# %% [markdown]
# ## Problem: Calculating mathematical expressions (2 points)
# 
# ### Use variables (1 point)
# 
# Create a variable called `x` with the value $\frac{1}{2}$.
# 
# ### Use the math module (1 point)
# 
# Import the `math` module (0.5 points). Calculate $x + cos(\pi)^2$ and assign your answer to a variabled called `y` (1 point).

# %% [markdown]
# ### Setup

# %%
x = None
y = None

# %% [markdown]
# ### Answer

# %%
import math
x = 1/2
y = 1.5

# %% [markdown]
# ### Result

# %%
if x is not None:
    # this should display your variable
    print(x, y)

    # this should not throw any errors
    assert x == 0.5
    assert y == 1.5 

# %% [markdown]
# ## Problem: making a list (2 points)
# 
# ### Create a list (1 point)
# 
# Create a list called `y` that has these items: 1, 2, 4, 8.
# 
# ### Add an item to a list (1 point)
# 
# Use the `append` method to add 16 onto the end of the list `y`.

# %% [markdown]
# ### Setup

# %%
y = None  # assign your answer to this variable

# %% [markdown]
# ### Answer

# %%
y = [1, 2, 4, 8]
y.append(16)

# %% [markdown]
# ### Result

# %%
if y is not None:
    # this should display your variable
    print(y)

    # this should not throw any errors
    assert y == [1, 2, 4, 8, 16]

# %% [markdown]
# ## Problem: accessing values in a list (2 points)
# 
# ### Index items in a list (1 point)
# 
# Below, a list `z` is defined. Use indexing to get the first item in `z` and assign it to a variable named `first_item` (0.5 points). Use indexing to get the last item in `z` and assign it to a variable named `last_item` (0.5 points).
# 
# ### Use slicing to access part of a list (1 point)
# 
# Use slicing to get the third and fourth items in the list `z` and assign it to a variable called `sub_list`.

# %% [markdown]
# ### Setup

# %%


# %% [markdown]
# ### Answer

# %%
z = [1, 2, 3, 4, 5]
first_item = z[0]
last_item = z[-1]
sub_list = z[2:4]

# %% [markdown]
# ### Result

# %%
vars = [first_item, last_item, sub_list]
if all([v is not None for v in vars]):
    # this should display your variables
    print(first_item)
    print(last_item)
    print(sub_list)

    # this should not throw any errors
    assert first_item == 1
    assert last_item == 5
    assert sub_list == [3, 4]

# %% [markdown]
# ## Problem: using a dictionary (2 points)
# 
# ### Create a dictionary (1 point)
# 
# Create a dict called `letters` with keys `"a"`, `"b"`, and `"c"`, which have values of `1`, `2`, and `3` respectively.
# 
# ### Access a value in a dictionary (1 point)
# 
# Use assignment (like `mydict[new_key] = new_value`) to add another key, `"d"`, with a value of `4`, to the `letters` dictionary.

# %% [markdown]
# ### Setup

# %%
letters = None

# %% [markdown]
# ### Answer

# %%
letters = {"a": 1, "b": 2, "c": 3}
letters["d"]= 4

# %% [markdown]
# ### Result

# %%
if letters is not None:
    # this should display your variable
    print(letters)

    # this should not throw any errors
    assert letters == {"a": 1, "b": 2, "c": 3, "d": 4}

# %% [markdown]
# ## Problem: accessing a dictionary (2 points)
# 
# ### Access a value from a dictionary (1 point)
# 
# Given a dictionary `participant_age` (defined below), use indexing to access the age of participant `"002"` and assign it to a variable called `age_002`.
# 
# ### Change a value in a dictionary (1 point)
# 
# Use assignment to change the age of participant `"003"` to 28.

# %% [markdown]
# ### Setup

# %%
participant_age = {"001": 32, "002": 23, "003": 18}
age_002 = None

# %% [markdown]
# ### Answer

# %%
age_002 = participant_age["002"]
participant_age["003"]= 28

# %% [markdown]
# ### Result

# %%
if age_002 is not None:
    # this should display your variables
    print(age_002)
    print(participant_age)

    # this should not throw any errors
    assert age_002 == 23
    assert participant_age == {"001": 32, "002": 23, "003": 28}

# %% [markdown]
# ## Problem: making a dictionary of lists (2 points)
# Say that you have data for three participants in a psychology study. Each participant has an identifier, age, and accuracy on a memory test.
# 
# | participant_id | age | accuracy |
# | -------------- | --- | -------- |
# | 001            | 24  | 0.85     |
# | 002            | 21  | 0.72     |
# | 003            | 33  | 0.92     |
# 
# Create a dictionary called `data` with three keys: `"participant_id"`, `"age"`, and `"accuracy"` (0.5 points). The values of each key should be a list with the values for that column in the above table, in order. The `participant_id` list should contain strings `"001"`, `"002"`, and `"003"` (0.5 points). The `age` list should contain integers 24, 21, and 33 (0.5 points). The `accuracy` list should contain floats 0.85, 0.72, and 0.92 (0.5 points).

# %% [markdown]
# ### Setup

# %%
data = None

# %% [markdown]
# ### Answer

# %%
data= {
    "participant_id" : ["001", "002", "003"],
    "age" : [24, 21, 33],
    "accuracy": [0.85, 0.72, 0.92]

}

# %% [markdown]
# ### Result

# %%
if data is not None:
    # this should print your variable
    print(data)

    # this should not throw any errors
    assert "participant_id" in data
    assert "age" in data
    assert "accuracy" in data
    assert data["participant_id"] == ["001", "002", "003"]
    assert data["age"] == [24, 21, 33]
    assert data["accuracy"] == [0.85, 0.72, 0.92]

# %% [markdown]
# ## Problem (graduate students): calculating expected forgetting (2 points)
# Use Python expressions and the `math` module to calculate the expected fraction of memorized material that will be forgotten after 100 minutes, according to [Ebbinghaus' formula](https://en.wikipedia.org/wiki/Forgetting_curve) for forgetting:
# 
# $b = \frac{k}{log_{10}(t)^{c} + k}$
# 
# with the values defined below.

# %% [markdown]
# ### Setup

# %%
t = 100  # 100 minutes
c = 1.25  # value that Ebbinghaus estimated based on his forgetting data
k = 1.84  # value that Ebbinghaus estimated based on his forgetting data
b = None

# %% [markdown]
# ### Answer

# %%
# your code here

# %% [markdown]
# ### Result

# %%
if b is not None:
    # this should display your variable
    print(b)

    # this should not throw any errors
    assert round(b, 3) == 0.436

# %% [markdown]
# ## Problem (graduate students): floating-point arithmetic (2 points)
# 
# When we run the below code, we get a strange result.

# %%
a = 0.1 + 0.2
a == 0.3

# %% [markdown]
# We know that $0.1 + 0.2 = 0.3$, but Python tells us that `a` does not equal `0.3`. This happens because Python usually uses binary coding (base 2) to represent floating-point values, sometimes resulting in inaccurate results.

# %% [markdown]
# ### Test the value of a floating-point number (1 point)
# 
# Read the Python [tutorial](https://docs.python.org/3/tutorial/floatingpoint.html) for an explanation of floating-point numbers.
# 
# If we want to test that `a` is very close to 0.3, how can we check this? The tutorial describes two methods, using `math.isclose` or `round`. Use one of them below. Your code should compare `a` to `0.3` and assign the result to a variable called `is_equal`.
# 
# ### Use a decimal representation (1 point)
# 
# Read about the [decimal module](https://docs.python.org/3.13/library/decimal.html), which represents numbers in base 10 for greater accuracy at the expense of speed. 
# 
# To create a decimal number, you can use the Decimal class (import it first, using `from decimal import Decimal`). If you create a variable using code like `Decimal(0.2)`, Python will create a float to represent `0.2`, representing it in binary first before converting to a decimal, which will cause the kind of inaccuracy we were trying to avoid. To make a precise decimal, instead pass a string to be converted to a decimal, like `Decimal("0.2")`.
# 
# Create variables `x = 0.1` and `y = 0.2` using the Decimal class, then calculate `z = x + y` (0.5 points). Use the equality operator `==` to test if `z` is equal to `0.3` (as a Decimal) and assign the result to a variable named `is_equal_decimal` (0.5 points).

# %% [markdown]
# ### Setup

# %%
is_equal = None
z = None
is_equal_decimal = None

# %% [markdown]
# ### Answer

# %%
# your code here

# %% [markdown]
# ### Result

# %%
vars = [is_equal, z, is_equal_decimal]
if all([v is not None for v in vars]):
    # this should display your variables
    print(is_equal)
    print(z)
    print(is_equal_decimal)

    # this should not throw any errors
    assert is_equal == True
    assert is_equal_decimal == True

# %% [markdown]
# ## Problem (graduate students): using JSON (2 points)
# 
# The built-in `json` module can be used to read JSON files or strings. Below, we define the text data corresonding to a JSON file using Python's triple quote (`"""`) syntax, which allows us to create a string with multiple lines of text.
# 
# ### Read a JSON string into a dictionary (1 point)
# 
# Read the provided string, `json_string`, into a Python dictionary called `descriptions` using the `json.loads` function. Run `help(json.loads)` or `json.loads?` to see information about the function in Jupyter.
# 
# ### Access the description of the age field (1 point)
# 
# Use dictionary indexing to access the `Description` field of the `age` field and assign it to a variable named `age_description`.

# %% [markdown]
# ### Setup

# %%
json_string = """
{
  "participant_id": {
    "Description": "Participant identifier"
  },
  "age": {
    "Description": "Age in years"
  },
  "sex": {
    "Description": "Sex self-rated by participant, M for male/F for female"
  }
}
"""
descriptions = None
age_description = None

# %% [markdown]
# ### Answer

# %%
# your code here

# %% [markdown]
# ### Result

# %%
vars = [descriptions, age_description]
if all([v is not None for v in vars]):
    # this should display your variables
    print(descriptions)
    print(age_description)

    # this should not throw any errors
    assert "participant_id" in descriptions
    assert "age" in descriptions
    assert "sex" in descriptions
    assert age_description == "Age in years"

# %% [markdown]
# ## Problem (graduate students): using sets (2 points)
# 
# Read about [sets](https://docs.python.org/3/tutorial/datastructures.html#sets) in the Python tutorial. 
# 
# ### Take the intersection of two sets (1 point)
# 
# Given two sets of participant identifiers, `participant_set1` and `participant_set2`, use the set operation `|` to create a set with all participants and assign it to a variable called `participants`.
# 
# ### Check for set membership (1 point)
# 
# Use the `in` keyword to test if participant `"004"` is in the combined set of participants. Assign the result to a variable called `is_004_included`.

# %% [markdown]
# ### Setup

# %%
participant_set1 = {"001", "002", "003"}
participant_set2 = {"004", "005", "006"}
participants = None
is_004_included = None

# %% [markdown]
# ### Answer

# %%
# your code here

# %% [markdown]
# ### Result

# %%
vars = [participants, is_004_included]
if all([v is not None for v in vars]):
    # this should display your variables
    print(participants)
    print(is_004_included)

    # this should not throw any errors
    assert participants == {"001", "002", "003", "004", "005", "006"}
    assert is_004_included == True


